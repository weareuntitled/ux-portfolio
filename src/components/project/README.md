# Project page primitives

Canonical project-page UI primitives live in this module (`src/components/project/*`).

Use these imports for hero and impact blocks:

- `@/components/project/ProjectCaseStudyHero`
- `@/components/project/ProjectImpactCards`

Legacy root-level paths in `src/components/*` are temporary re-exports and are deprecated.
