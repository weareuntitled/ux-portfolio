'use client';

import {
  StaggerContainer as BaseStaggerContainer,
  StaggerItem as BaseStaggerItem,
} from '@/components/animations/Stagger';

export const StaggerContainer = BaseStaggerContainer;
export const StaggerItem = BaseStaggerItem;

