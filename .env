NODE_ENV=production
PORT=3000

# In production, keep admin / edit functionality off by default.
EDIT_MODE=true
# === Payload core secrets (development only) ===
PAYLOAD_SECRET=ZV4EsLH25epAA444b1/Mh1jMfRmwzTt/+rfiGRKN/RjaujNDKCwFcBIAapbOZebt
PAYLOAD_PUBLIC_URL=http://localhost:3000

# === SQLite database path (local filesystem) ===
PAYLOAD_DB_SQLITE_PATH=./data/payload.db

# === Uploads directory (local filesystem) ===
PAYLOAD_UPLOADS_DIR=./uploads

# === Admin bootstrap user (optional, dev only) ===
ADMIN_DEFAULT_EMAIL=djdanep@gmail.com
ADMIN_DEFAULT_PASSWORD=Carlos95

# === Draft Mode / Preview ===
PREVIEW_SECRET=D@nielp170895

# === CORS / security (optional) ===
ALLOWED_ORIGINS=https://portfolio.example.com

# === Caddy / reverse proxy (used by Caddyfile) ===

APP_DOMAIN=portfolio.example.com
EMAIL_FOR_ACME=djdanep@gmail.com
NEXT_TELEMETRY_DISABLED=1
